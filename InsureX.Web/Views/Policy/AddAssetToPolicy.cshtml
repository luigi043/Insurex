@{ ViewData["Title"] = "Add Asset to Policy"; }

<div class="container-fluid">
    <h1 class="h3 mb-4 text-gray-800">Add Asset to Existing Policy</h1>
    @if (TempData["Error"] != null) { <div class="alert alert-danger">@TempData["Error"]</div> }
    @if (TempData["Success"] != null) { <div class="alert alert-success">@TempData["Success"]</div> }

    <div class="card shadow mb-4">
        <div class="card-header py-3"><h6 class="m-0 font-weight-bold text-primary">Add Asset</h6></div>
        <div class="card-body">
            <form asp-action="AddAssetToPolicy" method="post">
                @Html.AntiForgeryToken()

                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label>Policy Number</label>
                        <input type="text" name="PolicyNumber" class="form-control" placeholder="Enter existing policy number..." required />
                    </div>
                    <div class="form-group col-md-6">
                        <label>Asset Type</label>
                        <select name="AssetTypeId" id="assetTypeSelect" class="form-control selectpicker" data-live-search="true" required>
                            <option value="">-- Select Asset Type --</option>
                            @if (ViewBag.AssetTypes != null) {
                                foreach (var item in (List<KeyValuePair<string, string>>)ViewBag.AssetTypes) {
                                    <option value="@item.Key">@item.Value</option>
                                }
                            }
                        </select>
                    </div>
                </div>

                <div id="assetTypeFields"></div>

                <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Add Asset</button>
                <button type="reset" class="btn btn-secondary"><i class="fas fa-times"></i> Cancel</button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script>
    $('#assetTypeSelect').change(function () {
        var assetType = $(this).val();
        if (assetType) {
            $.get('@Url.Action("AssetTypePartial", "Asset")?assetType=' + assetType, function (html) {
                $('#assetTypeFields').html(html);
                formatCurrencyTextBox();
                DropDownSearch();
            });
        } else { $('#assetTypeFields').html(''); }
    });
</script>
}
